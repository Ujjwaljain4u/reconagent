services:
  - type: web
    name: reconagent-web
    env: python
    plan: free
    buildCommand: "pip install -e '.[web]'"
    startCommand: "uvicorn reconagent.web:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
